# Shared Module

This directory contains shared contracts, configurations, and utility modules used across backend, frontend, and ML layers.

## Structure
- `contracts/`: Data schemas and API contract schemas (owned by docs-contracts)
- `config/`: Global configurations
- `utils/`: Common utility functions
