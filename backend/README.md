# Backend Module

This directory contains the backend application scaffolding (FastAPI) for serving ranked offers.

## Structure
- `app/`: Application source code (owned by backend-scaffold)
- `tests/`: Backend test suite
